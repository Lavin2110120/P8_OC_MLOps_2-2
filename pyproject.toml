[project]
name = "projet-morel"
version = "0.1.0"
description = "Scoring Solution Client - Projet 6"
readme = "README.md"
requires-python = ">=3.10, <3.13"

dependencies = [
    "pandas >= 2.0.0",
    "numpy >= 1.24.0, < 2.1.0",
    "seaborn >= 0.13.0",
    "matplotlib >= 3.8.0, < 3.11.0", 
    "scikit-learn >= 1.4.0",
    "imbalanced-learn >= 0.12.0",
    "category_encoders >= 2.6.0",
    "xgboost >= 2.0.0, < 3.0.0",
    "lightgbm >= 4.0.0, < 5.0.0",     
    "mlflow >= 2.10.0",
    "sqlalchemy >= 2.0.0",           
    "ipykernel >= 6.29.0",
    "Jinja2 >= 3.1.0",
    "openpyxl >= 3.1.0"           
]