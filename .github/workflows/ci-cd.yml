name: CI/CD Pipeline - FastAPI Render

on:
  push:
    branches: [ main, test ]

jobs:
  test:
    name: Run Pytest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
      - run: |
          python -m pip install --upgrade pip
          pip install -e .
      - run: pytest

<<<<<<< HEAD
      - name: Run Pytest
<<<<<<< HEAD
=======
        env:
          DATABASE_URL: postgresql+asyncpg://p8_db_ba6y_user:xPkdpcurgzpMopfxc8JRAnvEspmurHib@dpg-d9q6o5lbedkc73b78mtg-a.frankfurt-postgres.render.com/p8_db_ba6y?ssl=require
>>>>>>> bc6e882 (add storage data with space postgresql)
=======
  deploy:
    name: Trigger Render Deployment
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Deploy on Render
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
          RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
>>>>>>> db4803c (update yaml with secrets, main.py with latency middleware, bugs fixed)
        run: |
          curl -X POST \
            -H "Authorization: Bearer $RENDER_API_KEY" \
            -H "Accept: application/json" \
            "https://api.render.com/v1/services/$RENDER_SERVICE_ID/deploys"